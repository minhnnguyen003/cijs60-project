import { authStateChanged } from "./user.js"

export async function createGame(gameName, gameUrl) {
    await firebase.firestore().collection(game)
}

export async function toUploadPage(callback) {
    this.
}